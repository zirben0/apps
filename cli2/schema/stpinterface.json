{
  "stpinterface": {
    "type" : "object",
    "properties" : {
      "prompt" : {
        "type": "string",
        "default" : "stp"
      },
      "cliname" : {
        "type" : "string",
        "description" : "name used in cli to describe ethernet",
        "default" : "stp"
      },
      "help" : {
        "type" : "string",
        "default": "Vlan Commands"
      },
      "commands" : {
        "type": "object",
        "description": "holds all related commands related to ethernet port",
        "properties": {
          "subcmd1": {
            "$ref": "file:/tmp/snaproute/cli/schema/StpPortMembers.json"
          }
        }
      }
    }
  }
}
