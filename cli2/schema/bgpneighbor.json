{
  "neighbor": {
    "type" : "object",
    "properties" : {
      "prompt" : {
        "type": "string",
        "default" : "neighbor"
      },
      "cliname" : {
        "type" : "string",
        "description" : "name used in cli to describe ethernet",
        "default" : "neighbor"
      },
       "help" : {
        "type" : "string",
        "default": "BGP Neighbor Info"
      },
      "value" : {
        "type" : "integer",
        "description" : "this is a trigger to cli that a command needs a value"
      },
      "commands" : {
        "type": "object",
        "description": "holds all related commands related to BGP neighbor",
        "properties": {
          "subcmd1": {
            "$ref": "file:/tmp/snaproute/cli/schema/BGPNeighborMembers.json"
          }
        }
      }
    }
  }
}
