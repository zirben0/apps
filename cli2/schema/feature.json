{
  "feature": {
    "type" : "object",
    "properties" : {
      "cliname" : {
        "type" : "string",
        "description" : "name used in cli to describe global features to be enabled",
        "default" : "feature"
      },
       "help" : {
        "type" : "string",
        "default": "Features available to enable"
      },
      "commands" : {
        "type": "object",
        "description": "holds all related commands related to ethernet port",
        "properties": {
          "subcmd1": {
            "$ref": "file:/tmp/snaproute/cli/schema/BfdGlobalMembers.json"
          }
        }
      }
    }
  }
}
